/* eslint-disable no-undef */
'use strict'
const Events = require('events');

const events = new Events();

module.exports = events;

/*
Vender alerts dirver to pickup
Driver pickups up and send in-transit to vender
Driver delivers and emits deliverd to vendor
vendor reads transit and says cool
vendor reads delivered and says thanks


const boyd = requier ('body.js)
describe('light, ()=>{
    it ('pupils dial), ()=.{
        const reaulst = pupil(payload)
        expect(result).toEqual('dialated)
    }
}),
*/

/*
const faker = require('faker')
randomName= faker.name.findName();
address = faker.address.streetAddress();

*/